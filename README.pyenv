
# Install pyenv
git clone https://github.com/pyenv/pyenv.git ~/.pyenv
# pyenv configuration set up in .zshenv
#
# pyenv will install various versions of python
pyenv install --list  # Show available versions to install

pipenv will then manage the package installation within that version?
